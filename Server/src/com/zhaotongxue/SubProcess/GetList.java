/*
package com.zhaotongxue.SubProcess;

import com.zhaotongxue.User;

import java.io.IOException;
import java.util.ArrayList;
*/
/**
 * @author zhao
 * @version 1.0
 */
/*
public class GetList {
    //Verison 1:return the list as string
    User user;

    public GetList(User user) throws IOException {
        this.user = user;
        this.getList();
    }

    public String getList() throws IOException{
        //获得用户列表并且发送
        ArrayList<User> userList= ListMaster.getUserList();
        String strUserlist = null;
        for(User u:userList) {
            strUserlist += u.getAddr() + "\n";
        }
        user.send(strUserlist);
    }
}
*/
